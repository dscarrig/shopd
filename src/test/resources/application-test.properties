# Test Configuration - No real GCS connection needed
spring.cloud.gcp.project-id=test-project
gcs.bucket.name=test-bucket

# Disable GCS auto-configuration in tests
spring.cloud.gcp.storage.enabled=false
